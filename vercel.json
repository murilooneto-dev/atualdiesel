{
  "installCommand": "npm install && npm --prefix backend install && npm --prefix frontend install",
  "buildCommand": "npm --prefix backend exec -- prisma generate --schema=backend/prisma/schema.prisma && npm --prefix backend run build && npm --prefix frontend run build",
  "outputDirectory": "frontend/dist",
  "functions": {
    "api/index.js": {
      "includeFiles": "{backend/dist/**,backend/node_modules/pdfkit/js/**}"
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
